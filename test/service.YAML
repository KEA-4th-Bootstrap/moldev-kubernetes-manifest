kind: Service
apiVersion: v1
metadata:
  name: test-service
spec:
  selector:
    app: test-canary
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30000 # 외부 클러스터에서 노출되는 포트
  type: NodePort # 외부 클러스터에서 인식하는 방법을 정의
