apiVersion: v1
kind: Service
metadata:
  name: test-service  # 이상한 문자 제거
spec:
  selector:
    app: test-canary
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30000
  type: NodePort
